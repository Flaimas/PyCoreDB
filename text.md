1. Слой данных (ООП, Dunder-методы, Сomplexity)Создай структуру данных для хранения записей.Класс Record: Представляет собой сущность с динамическими атрибутами.Реализуй магические методы, чтобы объекты можно было сравнивать (__eq__), выводить в читаемом виде (__str__, __repr__) и вызывать как функцию (__call__), которая возвращает dict с данными.Класс Database: Хранилище.Данные должны храниться внутри структур dict и set для обеспечения скорости поиска за $O(1)$. Ключами должны быть хэши или уникальные ID.Реализуй протокол контекстного менеджера (__enter__ / __exit__), чтобы при выходе из контекста (блок with) база автоматически "коммитила" изменения (например, выводила лог или симулировала сохранение).
2. Продвинутый Python (Декораторы, Генераторы, Итераторы) 
    Кастомный итератор: Реализуй внутри класса Database или отдельным классом возможность ленивой итерации по записям, удовлетворяющим определенному условию, не загружая всю выборку в память целиком (__iter__ и __next__).Генератор: Напиши генераторную функцию stream_data(data_source), которая с помощью yield порционно (батчами) отдает данные для симуляции потоковой обработки.Декораторы:Напиши декоратор @benchmark, который замеряет время выполнения функций БД и выводит его.Напиши параметризованный декоратор @access_control(role="admin"), который проверяет права доступа перед выполнением метода.
3. Множественное наследование и MROСоздай систему логирования для БД через примеси (mixins).У тебя должен быть базовый класс BaseEngine.Должны быть классы-примеси: FileLoggerMixin и ConsoleLoggerMixin.Создай финальный класс SmartDatabase, который наследуется от BaseEngine и обеих примесей.Условие: Спроектируй их так, чтобы при вызове метода log() срабатывали методы обоих миксинов в строго определенном порядке. В коде (в комментариях) ты должен расписать цепочку MRO для этого класса и объяснить, почему она именно такая.
4. Теоретический блок-тест (Память и GIL)Внутри этого же проекта создай файл theory.py или README.md, где ты сам для себя (или для воображаемого интервьюера) ответишь на следующие вопросы, подкрепив их кодом-демонстрацией:Garbage Collector: Напиши кусок кода, создающий циклическую ссылку (cyclic reference). С помощью модуля gc и sys.getrefcount продемонстрируй, как увеличивается счетчик ссылок и в какой момент объект удаляется только сборщиком мусора, а не стандартным подсчетом ссылок.GIL: Напиши два скрипта для тяжелых вычислений (CPU-bound задача, например, подсчет огромного числа Фибоначчи). Один запускает вычисления в 2 потоках (threading), второй — в 2 процессах (multiprocessing). Замерь время и объясни, почему потоки в Python не дали прироста скорости, связав это с работой GIL.🛠 Критерии приемки (Checklist для проверки)Complexity: Ты можешь четко объяснить, почему поиск записи по ID в твоей БД занимает $O(1)$, а фильтрация по кастомному полю — $O(n)$.Магия: Класс базы данных поддерживает синтаксис with Database() as db: и for record in db:.MRO: Метод super() в наследовании используется осознанно, вызов SmartDatabase.__mro__ не вызывает конфликтов (TypeError: Cannot create a consistent method resolution...).Генераторы: Память при ленивой фильтрации не расходуется под весь объем данных.Когда напишешь этот проект, ты закроешь сразу все практические и теоретические вопросы этого этапа роадмапа. Готов взяться за проектирование структуры?